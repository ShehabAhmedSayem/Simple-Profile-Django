# SimpleProfile-Django
Simple Login Logout system built with Django

Checkout this repo, install dependencies, then start the server:

    > pip install -r requirements.txt
    > python manage.py runserver
    
    
    
